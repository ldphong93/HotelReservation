spring:
  cloud:
    config:
      uri: http://localhost:8888
      username: admin-phong
      password: pass-phong
  config:
#    import: configserver:http://localhost:8888

    activate:
      on-profile: security
  application:
    name: hotel-service